# Next.js migration scaffold

This folder contains a minimal Next.js scaffold to begin migrating the Vite app.

Quick start:

```bash
cd next-app
npm install
npm run dev
```

Notes:
- The Vite app remains in the repository root and is not modified.
- We'll migrate components/pages incrementally.
